import "io";
import "math";

int globalA = 5, globalB;
float[] globalArray = [1.5, 2.0, 3.5];
map<string, int> edades = {
    "ana": 20,
    "bob": 25,
    "carla": 30
};

fn sumar(int a, int b): int {
    int resultado = a + b * 7 / (12 - 9) + b;
    return resultado;
}

fn mostrarEdad(string nombre) {
    if [nombre == "ana"] then
        print("Edad de Ana: ", edades["ana"]);
    elif [nombre == "bob"] then
        print("Edad de Bob: ", edades["bob"]);
    else then
    	print("Desconocido");
    fi
}

fn main() {
    int i = 0, total = 0;
    float promedio;
    map<string, int> calificaciones;

    // Asignación a map
    calificaciones = {
        "matematicas": 90,
        "lenguaje": 85,
        "historia": 78
    };

    // While loop
    while [i < 3] do
        total = total + i;
        i++;
    done

    // Until (do-while invertido)
    i = 0;
    until [i >= 3] do
        print("Iteración: ", i);
        i++;
    done

    // For con sintaxis tipo C
    for (int j = 0; j < 5; j++) do
        if [j == 2] then
            continue;
        fi
        print("Valor de j: ", j);
    done

    // For-each
    for string nombre in edades do
        mostrarEdad(nombre);
    done

    // Switch
    int opcion = 2;
    switch(opcion) then
        case [1] do
            print("Opción 1");
	done
        case [2] do
            print("Opción 2");
	done
	default do
            print("Opción desconocida");
	done
    fi

    // Arrays
    int[3] numeros;
    numeros = [10, 20, 30];
    print("Primer número:", numeros[0]);

    // Expresiones con precedencia
    promedio = (globalA + total * 2) / 3.0;

    // Llamada a función con asignación
    int resultado = sumar(4, 6);
    print("Resultado de la suma:", resultado);

    // Ejemplo de map y acceso
    print("Edad de Carla:", edades["carla"]);

    // Pass (para compatibilidad sintáctica)
    pass;
}
