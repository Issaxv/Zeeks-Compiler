fn main() {
    int a = 5;
    float b = 2.5;
    string c = "Hola";

    a = (a + 3) * 2;         // ✅
    b = a / 3.0;             // ✅
    c = a + b;               // ❌ suma inválida entre int y float → debería ser float
    a = b * c;               // ❌ multiplicación inválida entre float y string
}
